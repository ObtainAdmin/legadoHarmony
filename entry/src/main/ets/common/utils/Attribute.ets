

/**
 * 属性枚举 - 定义应用中的常量属性值
 * 
 * 功能：提供统一的属性常量定义，用于标识特定的UI元素或功能
 * 使用场景：组件属性设置、样式配置、布局参数等
 * 注意：目前只有一个Header属性，可能用于预留电池空间或头部区域
 */
export enum Attribute {
  /**
   * 头部属性
   * 功能：标识头部区域或预留电池空间
   * 值：20（可能表示像素、DP或其他单位）
   * 使用场景：
   *   - 预留电池空间：在HarmonyOS设备上为电池图标预留空间
   *   - 头部高度：设置页面头部区域的高度
   *   - 其他需要常量20的场景
   * 注意：注释中提到"预留电池空间"，可能用于适配不同设备的电池图标区域
   */
  Header = 20,
}
